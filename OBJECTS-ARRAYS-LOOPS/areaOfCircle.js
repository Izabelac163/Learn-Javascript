// 1. write a function that calculates the area of a circle - PI * R * R

function findArea(Radius) {
  var area = 3.14 * Radius * Radius;
  return area;
}

var areaFound = findArea(6);
console.log(areaFound);
